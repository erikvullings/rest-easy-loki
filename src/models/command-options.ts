export interface ICommandOptions {
  /** Show the manual */
  help: boolean;
  /** Show verbose output */
  verbose: boolean;
  /** Port to use */
  port: number;
  /** Enable CORS */
  cors: boolean;
}
